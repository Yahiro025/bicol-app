# Environment variables
.env
.env.*

# Python
venv/
__pycache__/
*.py[cod]
*$py.class

# Node.js
node_modules/
.next/

# macOS
.DS_Store

If you ever need to share those tool‑specific files (e.g., to reproduce a CI environment), you can temporarily remove the lines or add a comment marker (# keep‑in‑repo) to make them tracked again. For normal development, keep
# Antigravity tooling
.antigravitycli/
# Gemini CLI
.gemini/
# Impeccable design
.impeccable/

# Scrapers
ai_wiktionary_scraper.py
wiktionary_scraper.py

tsconfig.tsbuildinfo
